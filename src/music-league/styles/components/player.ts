/**
 * @file player.ts
 * @description Spotify Player styles for Music League components
 */

export const player = {
  container: 'relative w-full',
  loadingOverlay:
    'absolute inset-0 bg-black/50 backdrop-blur-sm rounded-lg flex items-center justify-center z-10',
} as const;
